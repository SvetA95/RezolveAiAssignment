# Tests mutate a property on a shared environment (see BaseApiTest). Parallel
# execution is explicitly disabled rather than relying on the JUnit 5 default,
# since enabling it later (e.g. via a CI override) would make these tests flaky.
junit.jupiter.execution.parallel.enabled=false
